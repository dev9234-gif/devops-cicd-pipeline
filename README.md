# 🚀 CI/CD Pipeline using Jenkins, Docker & AWS

This project demonstrates a complete CI/CD pipeline where a Node.js application is automatically built, containerized, and deployed to AWS EC2 using Jenkins and Docker.

The pipeline includes:
- Continuous Integration (CI)
- Continuous Deployment (CD)
- Docker containerization
- AWS ECR for image storage
